/** Results — intentionally a placeholder until HUD + Population are validated. */
export default function ResultsScreen() {
  return (
    <div className="screen">
      <div className="card" style={{ textAlign: 'center', padding: '34px 20px' }}>
        <div style={{ fontSize: 15, fontWeight: 700, marginBottom: 6 }}>
          Results isn't a priority for this pass
        </div>
        <div style={{ color: 'var(--text-dim)', fontSize: 13, lineHeight: 1.6 }}>
          Session P&amp;L, bankroll and trend charts will live here — deferred until the Live
          HUD and Population layers are validated at the table.
        </div>
      </div>
    </div>
  );
}
